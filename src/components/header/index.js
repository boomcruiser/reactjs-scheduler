import React from "react";
import { Typography } from "@material-ui/core";
const Header = () => {
  return <Typography variant="h2">React Scheduler Application</Typography>;
};
export default Header;
